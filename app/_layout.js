import { View } from 'react-native';

export default function Layout() {
  // 이 파일은 expo-router의 파일 기반 라우팅을 비활성화하기 위해 의도적으로 비워 두었습니다.
  // 모든 내비게이션 로직은 @react-navigation을 사용하여 App.js에 위임됩니다.
  return <View />;
}