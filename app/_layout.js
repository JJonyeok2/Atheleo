import { View } from 'react-native';

export default function Layout() {
  // This file is intentionally left blank to disable expo-router's file-based routing
  // and delegate all navigation logic to App.js using @react-navigation.
  return <View />;
}